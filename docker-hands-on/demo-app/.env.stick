DATABASE_URL=postgresql://innocent:Password12345@localhost:5432/dockersample
PORT=8000

postgresql://admin:mysecretpassword@localhost:5438/demo-app

docker run -d \
  --name local-db-a \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -e POSTGRES_USER=admin \
  -e POSTGRES_DB=demo-app-a \
  -p 5438:5432 \
  --network obn-net \
  postgres:alpine